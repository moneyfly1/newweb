# 备份目录（不忽略代码文件，仅忽略备份产物）
backups/
*.backup
backup_*/
# 环境与敏感配置（保留 .env.example 作模板）
.env
.env.local
.env.*.local

# macOS
.DS_Store

# 构建产物与二进制
/server
/cboard
*.exe
*.dll
*.so
*.dylib

# 前端
frontend/node_modules/
frontend/dist/

# 数据库与备份（不上传至 GitHub）
*.db
*.db-shm
*.db-wal
cboard.db
cboard.db-shm
cboard.db-wal
backups/
# uploads 目录及模板可提交（temp.yaml 为 Clash 模板）
# GeoIP/地理数据库为运行时下载的二进制数据，不提交
uploads/config/*.dat
uploads/config/*.mmdb
uploads/config/*.metadb
uploads/config/stash_temp.yaml
# GitHub 文件同步产物（运行时下载，不入库）
uploads/nodes/

# IDE / 编辑器
.idea/
.vscode/
*.swp
*.swo
.claude/

# Go
*.test
*.out
vendor/

# 日志与临时文件
*.log
tmp/
temp/
*.pid
